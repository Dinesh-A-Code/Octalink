import type { Testimonial } from "@/types/content";

/**
 * Intentionally empty. The section renders an empty state until real,
 * attributable client quotes exist — add objects here and the carousel
 * replaces the empty state automatically. Never populate with invented ones.
 */
export const testimonials: Testimonial[] = [];
